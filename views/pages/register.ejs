<%- include('../layouts/main', { content: `
<div class="min-h-screen flex items-center justify-center py-4 sm:py-8 px-3 sm:px-4 lg:px-8">
    <div class="max-w-md w-full space-y-4 sm:space-y-6 lg:space-y-8 bg-white dark:bg-gray-800 p-4 sm:p-6 lg:p-8 rounded-lg sm:rounded-xl shadow-lg border border-gray-200 dark:border-gray-700">
        <div class="text-center">
            <h2 class="section-title text-2xl sm:text-3xl text-gray-900 dark:text-white">Create Account</h2>
            <p class="mt-2 text-xs sm:text-sm text-gray-600 dark:text-gray-400">
                Already have an account? 
                <a href="/login" class="text-emerald-600 dark:text-emerald-400 hover:text-emerald-700 dark:hover:text-emerald-300 font-medium transition-colors">
                    Sign in here
                </a>
            </p>
        </div>
        
        <!-- Error Message -->
        <div id="errorMessage" class="hidden bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 text-red-700 dark:text-red-400 px-3 sm:px-4 py-2 sm:py-3 rounded-lg error-message text-sm">
            <p id="errorText"></p>
        </div>
        
        <!-- Success Message -->
        <div id="successMessage" class="hidden bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800 text-green-700 dark:text-green-400 px-3 sm:px-4 py-2 sm:py-3 rounded-lg text-sm">
            <p id="successText"></p>
        </div>
        
        <form id="registerForm" class="mt-4 sm:mt-6 lg:mt-8 space-y-4 sm:space-y-5">
            <div>
                <label for="full_name" class="block text-xs sm:text-sm font-medium text-gray-700 dark:text-gray-300 mb-1.5 sm:mb-2">
                    Full Name <span class="text-red-500">*</span>
                </label>
                <input 
                    id="full_name" 
                    name="full_name" 
                    type="text" 
                    required 
                    autocomplete="name"
                    minlength="2"
                    class="w-full px-3 sm:px-4 py-2.5 sm:py-3 text-sm sm:text-base border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 dark:focus:ring-emerald-400 dark:focus:border-emerald-400 transition-colors"
                    placeholder="John Doe"
                >
            </div>
            
            <div>
                <label for="email" class="block text-xs sm:text-sm font-medium text-gray-700 dark:text-gray-300 mb-1.5 sm:mb-2">
                    Email Address <span class="text-red-500">*</span>
                </label>
                <div class="flex gap-2">
                    <input 
                        id="email" 
                        name="email" 
                        type="text" 
                        required 
                        autocomplete="email"
                        inputmode="email"
                        class="flex-1 px-3 sm:px-4 py-2.5 sm:py-3 text-sm sm:text-base border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 dark:focus:ring-emerald-400 dark:focus:border-emerald-400 transition-colors"
                        placeholder="you@example.com"
                    >
                    <button 
                        type="button"
                        id="verifyEmailBtn"
                        class="px-3 sm:px-4 py-2.5 sm:py-3 text-sm sm:text-base bg-emerald-600 hover:bg-emerald-700 dark:bg-emerald-500 dark:hover:bg-emerald-600 text-white rounded-lg transition-colors whitespace-nowrap disabled:opacity-50 disabled:cursor-not-allowed"
                    >
                        <span id="verifyEmailBtnText">Verify Email</span>
                    </button>
                </div>
                <p id="emailVerificationStatus" class="mt-1 text-xs hidden"></p>
            </div>
            
            <!-- OTP Verification Section -->
            <div id="otpVerificationSection" class="hidden space-y-3">
                <div>
                    <label class="block text-xs sm:text-sm font-medium text-gray-700 dark:text-gray-300 mb-1.5 sm:mb-2">
                        Enter 6-Digit OTP <span class="text-red-500">*</span>
                    </label>
                    <div class="flex gap-2">
                        <input 
                            id="otpInput" 
                            type="text" 
                            maxlength="6"
                            inputmode="numeric"
                            pattern="[0-9]{6}"
                            class="flex-1 px-3 sm:px-4 py-2.5 sm:py-3 text-sm sm:text-base border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 dark:focus:ring-emerald-400 dark:focus:border-emerald-400 transition-colors text-center tracking-widest text-lg font-semibold"
                            placeholder="000000"
                        >
                        <button 
                            type="button"
                            id="verifyOtpBtn"
                            class="px-3 sm:px-4 py-2.5 sm:py-3 text-sm sm:text-base bg-emerald-600 hover:bg-emerald-700 dark:bg-emerald-500 dark:hover:bg-emerald-600 text-white rounded-lg transition-colors whitespace-nowrap disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            Verify
                        </button>
                    </div>
                    <div class="mt-2 flex items-center justify-between">
                        <p id="otpStatus" class="text-xs text-gray-500 dark:text-gray-400"></p>
                        <button 
                            type="button"
                            id="resendOtpBtn"
                            class="text-xs text-emerald-600 dark:text-emerald-400 hover:underline disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            Resend OTP
                        </button>
                    </div>
                </div>
            </div>
            
            <div>
                <label for="phone" class="block text-xs sm:text-sm font-medium text-gray-700 dark:text-gray-300 mb-1.5 sm:mb-2">
                    Mobile Number <span class="text-red-500">*</span>
                </label>
                <div class="flex">
                    <div class="flex items-center px-2 sm:px-3 lg:px-4 py-2.5 sm:py-3 text-sm sm:text-base border border-r-0 border-gray-300 dark:border-gray-600 rounded-l-lg bg-gray-50 dark:bg-gray-700 text-gray-700 dark:text-gray-300 font-medium whitespace-nowrap">
                        +91
                    </div>
                    <input 
                        id="phone" 
                        name="phone" 
                        type="tel" 
                        required
                        autocomplete="tel"
                        maxlength="10"
                        pattern="[0-9]{10}"
                        inputmode="numeric"
                        class="flex-1 px-3 sm:px-4 py-2.5 sm:py-3 text-sm sm:text-base border border-gray-300 dark:border-gray-600 rounded-r-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 dark:focus:ring-emerald-400 dark:focus:border-emerald-400 transition-colors"
                        placeholder="1234567890"
                    >
                </div>
                <p class="mt-1 text-xs text-gray-500 dark:text-gray-400">Enter 10-digit mobile number</p>
            </div>
            
            <div>
                <label class="block text-xs sm:text-sm font-medium text-gray-700 dark:text-gray-300 mb-2 sm:mb-3">
                    I am a <span class="text-red-500">*</span>
                </label>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 sm:gap-4">
                    <label class="flex items-start sm:items-center p-3 sm:p-4 border-2 border-gray-200 dark:border-gray-600 rounded-lg cursor-pointer hover:border-emerald-500 dark:hover:border-emerald-500 transition-colors has-[:checked]:border-emerald-500 has-[:checked]:bg-emerald-50 dark:has-[:checked]:bg-emerald-900/20">
                        <input type="radio" name="role" value="buyer" required class="mt-0.5 sm:mt-0 mr-2 sm:mr-3 h-4 w-4 text-emerald-600 focus:ring-emerald-500 flex-shrink-0">
                        <div class="flex-1">
                            <div class="font-semibold text-sm sm:text-base text-gray-900 dark:text-white">Buyer</div>
                            <div class="text-xs text-gray-500 dark:text-gray-400 mt-0.5">Looking to buy property</div>
                        </div>
                    </label>
                    <label class="flex items-start sm:items-center p-3 sm:p-4 border-2 border-gray-200 dark:border-gray-600 rounded-lg cursor-pointer hover:border-emerald-500 dark:hover:border-emerald-500 transition-colors has-[:checked]:border-emerald-500 has-[:checked]:bg-emerald-50 dark:has-[:checked]:bg-emerald-900/20">
                        <input type="radio" name="role" value="seller" required class="mt-0.5 sm:mt-0 mr-2 sm:mr-3 h-4 w-4 text-emerald-600 focus:ring-emerald-500 flex-shrink-0">
                        <div class="flex-1">
                            <div class="font-semibold text-sm sm:text-base text-gray-900 dark:text-white">Seller</div>
                            <div class="text-xs text-gray-500 dark:text-gray-400 mt-0.5">Want to sell property</div>
                        </div>
                    </label>
                </div>
            </div>
            
            <div>
                <label for="password" class="block text-xs sm:text-sm font-medium text-gray-700 dark:text-gray-300 mb-1.5 sm:mb-2">
                    Password <span class="text-red-500">*</span>
                </label>
                <div class="relative">
                    <input 
                        id="password" 
                        name="password" 
                        type="password" 
                        required 
                        autocomplete="new-password"
                        minlength="6"
                        class="w-full px-3 sm:px-4 py-2.5 sm:py-3 pr-10 sm:pr-12 text-sm sm:text-base border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 dark:focus:ring-emerald-400 dark:focus:border-emerald-400 transition-colors"
                        placeholder="At least 6 characters"
                    >
                    <button 
                        type="button"
                        onclick="togglePasswordVisibility('password')"
                        class="absolute right-2 sm:right-3 top-1/2 transform -translate-y-1/2 text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300 p-1"
                        aria-label="Toggle password visibility"
                    >
                        <svg id="passwordEye" class="w-4 h-4 sm:w-5 sm:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                        </svg>
                    </button>
                </div>
                <p class="mt-1 text-xs text-gray-500 dark:text-gray-400">Must be at least 6 characters long</p>
            </div>
            
            <div>
                <label for="confirmPassword" class="block text-xs sm:text-sm font-medium text-gray-700 dark:text-gray-300 mb-1.5 sm:mb-2">
                    Confirm Password <span class="text-red-500">*</span>
                </label>
                <div class="relative">
                    <input 
                        id="confirmPassword" 
                        name="confirmPassword" 
                        type="password" 
                        required 
                        autocomplete="new-password"
                        class="w-full px-3 sm:px-4 py-2.5 sm:py-3 pr-10 sm:pr-12 text-sm sm:text-base border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 dark:focus:ring-emerald-400 dark:focus:border-emerald-400 transition-colors"
                        placeholder="Re-enter your password"
                    >
                    <button 
                        type="button"
                        onclick="togglePasswordVisibility('confirmPassword')"
                        class="absolute right-2 sm:right-3 top-1/2 transform -translate-y-1/2 text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300 p-1"
                        aria-label="Toggle password visibility"
                    >
                        <svg id="confirmPasswordEye" class="w-4 h-4 sm:w-5 sm:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                        </svg>
                    </button>
                </div>
            </div>
            
            <div class="flex items-start">
                <input 
                    id="terms" 
                    name="terms" 
                    type="checkbox" 
                    required
                    class="mt-0.5 sm:mt-1 h-4 w-4 text-emerald-600 focus:ring-emerald-500 border-gray-300 dark:border-gray-600 rounded flex-shrink-0"
                >
                <label for="terms" class="ml-2 sm:ml-3 block text-xs sm:text-sm text-gray-700 dark:text-gray-300 leading-relaxed">
                    I agree to the 
                    <a href="#" class="text-emerald-600 dark:text-emerald-400 hover:underline">Terms and Conditions</a>
                    and 
                    <a href="#" class="text-emerald-600 dark:text-emerald-400 hover:underline">Privacy Policy</a>
                </label>
            </div>
            
            <div>
                <button 
                    type="submit" 
                    id="submitBtn"
                    class="button-text w-full px-4 py-2.5 sm:py-3 text-sm sm:text-base bg-emerald-600 hover:bg-emerald-700 dark:bg-emerald-500 dark:hover:bg-emerald-600 text-white rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:ring-offset-2 dark:focus:ring-offset-gray-800 disabled:opacity-50 disabled:cursor-not-allowed flex items-center justify-center touch-manipulation"
                    disabled
                >
                    <span id="submitText">Create Account</span>
                    <span id="submitSpinner" class="hidden spinner ml-2"></span>
                </button>
                <p id="signupHint" class="mt-2 text-xs text-gray-500 dark:text-gray-400 text-center">
                    Please verify your email first
                </p>
            </div>
        </form>
    </div>
</div>

<script>
    function togglePasswordVisibility(inputId) {
        const input = document.getElementById(inputId);
        const eye = document.getElementById(inputId + 'Eye');
        const type = input.getAttribute('type') === 'password' ? 'text' : 'password';
        input.setAttribute('type', type);
        
        // Update eye icon
        if (type === 'text') {
            eye.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.543-7a9.97 9.97 0 011.563-3.029m5.858.908a3 3 0 114.243 4.243M9.878 9.878l4.242 4.242M9.88 9.88l-3.29-3.29m7.532 7.532l3.29 3.29M3 3l3.59 3.59m0 0A9.953 9.953 0 0112 5c4.478 0 8.268 2.943 9.543 7a10.025 10.025 0 01-4.132 5.411m0 0L21 21"></path>';
        } else {
            eye.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>';
        }
    }
    
    function showError(message) {
        // Hide inline messages
        const errorDiv = document.getElementById('errorMessage');
        const successDiv = document.getElementById('successMessage');
        if (errorDiv) errorDiv.classList.add('hidden');
        if (successDiv) successDiv.classList.add('hidden');
        
        // Show toast notification
        if (typeof window.showError === 'function') {
            window.showError(message);
        }
    }
    
    function showSuccess(message) {
        // Hide inline messages
        const errorDiv = document.getElementById('errorMessage');
        const successDiv = document.getElementById('successMessage');
        if (errorDiv) errorDiv.classList.add('hidden');
        if (successDiv) successDiv.classList.add('hidden');
        
        // Show toast notification
        if (typeof window.showSuccess === 'function') {
            window.showSuccess(message);
        }
    }
    
    function setLoading(isLoading) {
        const submitBtn = document.getElementById('submitBtn');
        const submitText = document.getElementById('submitText');
        const submitSpinner = document.getElementById('submitSpinner');
        
        if (isLoading) {
            submitBtn.disabled = true;
            submitText.textContent = 'Creating account...';
            submitSpinner.classList.remove('hidden');
        } else {
            submitBtn.disabled = false;
            submitText.textContent = 'Create Account';
            submitSpinner.classList.add('hidden');
        }
    }
    
    function validateForm() {
        const password = document.getElementById('password').value;
        const confirmPassword = document.getElementById('confirmPassword').value;
        const email = document.getElementById('email').value;
        const fullName = document.getElementById('full_name').value;
        const phone = document.getElementById('phone').value;
        
        // Validate password length
        if (password.length < 6) {
            showError('Password must be at least 6 characters long.');
            return false;
        }
        
        // Validate password match
        if (password !== confirmPassword) {
            showError('Passwords do not match. Please try again.');
            return false;
        }
        
        // Validate email format - simple regex without curly braces to avoid EJS parsing issues
        const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        if (!emailRegex.test(email)) {
            showError('Please enter a valid email address.');
            return false;
        }
        
        // Validate full name
        if (fullName.length < 2) {
            showError('Full name must be at least 2 characters long.');
            return false;
        }
        
        // Validate phone number - must be exactly 10 digits
        const phoneRegex = /^[0-9]{10}$/;
        if (!phone || !phoneRegex.test(phone)) {
            showError('Please enter a valid 10-digit mobile number.');
            return false;
        }
        
        return true;
    }
    
    // Phone number input validation - only allow numbers and limit to 10 digits
    document.getElementById('phone').addEventListener('input', function(e) {
        // Remove any non-digit characters
        let value = e.target.value.replace(/\D/g, '');
        
        // Limit to 10 digits
        if (value.length > 10) {
            value = value.slice(0, 10);
        }
        
        e.target.value = value;
        
        // Update border color based on validation
        if (value.length === 10) {
            e.target.classList.remove('border-red-500', 'dark:border-red-500');
            e.target.classList.add('border-emerald-500', 'dark:border-emerald-500');
        } else if (value.length > 0) {
            e.target.classList.remove('border-emerald-500', 'dark:border-emerald-500');
            e.target.classList.add('border-red-500', 'dark:border-red-500');
        } else {
            e.target.classList.remove('border-red-500', 'dark:border-red-500', 'border-emerald-500', 'dark:border-emerald-500');
            e.target.classList.add('border-gray-300', 'dark:border-gray-600');
        }
    });
    
    // Prevent non-numeric input
    document.getElementById('phone').addEventListener('keypress', function(e) {
        // Allow: backspace, delete, tab, escape, enter
        if ([8, 9, 27, 13, 46].indexOf(e.keyCode) !== -1 ||
            // Allow: Ctrl+A, Ctrl+C, Ctrl+V, Ctrl+X
            (e.keyCode === 65 && e.ctrlKey === true) ||
            (e.keyCode === 67 && e.ctrlKey === true) ||
            (e.keyCode === 86 && e.ctrlKey === true) ||
            (e.keyCode === 88 && e.ctrlKey === true)) {
            return;
        }
        // Ensure that it is a number and stop the keypress
        if ((e.shiftKey || (e.keyCode < 48 || e.keyCode > 57)) && (e.keyCode < 96 || e.keyCode > 105)) {
            e.preventDefault();
        }
    });
    
    // Email verification state
    let emailVerified = false;
    let emailForVerification = '';
    
    // Verify Email Button Handler
    document.getElementById('verifyEmailBtn').addEventListener('click', async function() {
        const emailInput = document.getElementById('email');
        let email = emailInput.value.trim();
        
        // Remove any leading/trailing whitespace and normalize
        email = email.toLowerCase().trim();
        
        if (!email) {
            showError('Please enter your email address first');
            emailInput.focus();
            return;
        }
        
        // Validate email format - simple regex without curly braces to avoid EJS parsing issues
        // This regex allows dots in the local part (before @) which is valid
        const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        
        // Also check for basic structure
        if (!email.includes('@') || !email.includes('.')) {
            showError('Please enter a valid email address');
            emailInput.focus();
            return;
        }
        
        if (!emailRegex.test(email)) {
            showError('Please enter a valid email address');
            emailInput.focus();
            return;
        }
        
        // Update input with normalized email
        if (emailInput.value !== email) {
            emailInput.value = email;
        }
        
        emailForVerification = email;
        const btn = this;
        const btnText = document.getElementById('verifyEmailBtnText');
        const originalText = btnText.textContent;
        
        btn.disabled = true;
        btnText.textContent = 'Sending...';
        
        try {
            const response = await fetch('/api/auth/send-verification-otp', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email })
            });
            
            const result = await response.json();
            
            if (response.ok) {
                showSuccess('OTP sent to your email! Please check your inbox.');
                document.getElementById('otpVerificationSection').classList.remove('hidden');
                document.getElementById('otpInput').focus();
                updateEmailVerificationStatus('OTP sent! Check your email.', 'text-emerald-600 dark:text-emerald-400');
            } else {
                // Check if email already exists/verified
                if (result.code === 'EMAIL_ALREADY_EXISTS' || result.message?.includes('already registered') || result.message?.includes('already verified') || result.error?.includes('already registered')) {
                    const errorMsg = 'This email is already registered. Please log in with your password.';
                    showError(errorMsg);
                    updateEmailVerificationStatus('Email already exists. Please log in.', 'text-red-600 dark:text-red-400');
                    // Suggest login after showing error
                    setTimeout(() => {
                        if (confirm('This email is already registered. Would you like to go to the login page?')) {
                            window.location.href = '/login?email=' + encodeURIComponent(email);
                        }
                    }, 1500);
                } else {
                    showError(result.error || result.message || 'Failed to send OTP. Please try again.');
                }
                btn.disabled = false;
                btnText.textContent = originalText;
            }
        } catch (error) {
            console.error('Send OTP error:', error);
            showError('An error occurred. Please try again.');
            btn.disabled = false;
            btnText.textContent = originalText;
        }
    });
    
    // Verify OTP Button Handler
    document.getElementById('verifyOtpBtn').addEventListener('click', async function() {
        const otpInput = document.getElementById('otpInput');
        const otp = otpInput.value.trim();
        
        if (!otp || otp.length !== 6) {
            showError('Please enter a valid 6-digit OTP');
            otpInput.focus();
            return;
        }
        
        if (!/^\d{6}$/.test(otp)) {
            showError('OTP must contain only numbers');
            otpInput.focus();
            return;
        }
        
        const btn = this;
        const originalText = btn.textContent;
        
        btn.disabled = true;
        btn.textContent = 'Verifying...';
        document.getElementById('otpStatus').textContent = '';
        
        try {
            const response = await fetch('/api/auth/verify-email', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: emailForVerification, otp })
            });
            
            const result = await response.json();
            
            if (response.ok) {
                emailVerified = true;
                showSuccess('Email verified successfully!');
                updateEmailVerificationStatus('✓ Email verified', 'text-green-600 dark:text-green-400');
                document.getElementById('otpVerificationSection').classList.add('hidden');
                document.getElementById('submitBtn').disabled = false;
                document.getElementById('signupHint').textContent = 'Email verified! You can now create your account.';
                document.getElementById('signupHint').classList.remove('text-gray-500', 'dark:text-gray-400');
                document.getElementById('signupHint').classList.add('text-green-600', 'dark:text-green-400');
            } else {
                showError(result.error || 'Invalid OTP. Please try again.');
                otpInput.value = '';
                otpInput.focus();
                btn.disabled = false;
                btn.textContent = originalText;
            }
        } catch (error) {
            console.error('Verify OTP error:', error);
            showError('An error occurred. Please try again.');
            btn.disabled = false;
            btn.textContent = originalText;
        }
    });
    
    // Resend OTP Button Handler
    document.getElementById('resendOtpBtn').addEventListener('click', async function() {
        if (!emailForVerification) {
            showError('Please enter and verify your email first');
            return;
        }
        
        const btn = this;
        btn.disabled = true;
        btn.textContent = 'Sending...';
        
        try {
            const response = await fetch('/api/auth/send-verification-otp', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: emailForVerification })
            });
            
            const result = await response.json();
            
            if (response.ok) {
                showSuccess('OTP resent successfully!');
                document.getElementById('otpInput').value = '';
                document.getElementById('otpInput').focus();
                btn.textContent = 'Resend OTP';
                // Disable for 60 seconds
                setTimeout(() => {
                    btn.disabled = false;
                }, 60000);
            } else {
                showError(result.error || 'Failed to resend OTP. Please try again.');
                btn.disabled = false;
                btn.textContent = 'Resend OTP';
            }
        } catch (error) {
            console.error('Resend OTP error:', error);
            showError('An error occurred. Please try again.');
            btn.disabled = false;
            btn.textContent = 'Resend OTP';
        }
    });
    
    // OTP Input - Auto format and verify on 6 digits
    document.getElementById('otpInput').addEventListener('input', function(e) {
        let value = e.target.value.replace(/\D/g, '');
        if (value.length > 6) value = value.slice(0, 6);
        e.target.value = value;
        
        // Auto-verify when 6 digits entered
        if (value.length === 6) {
            document.getElementById('verifyOtpBtn').click();
        }
    });
    
    // Email input change - reset verification if email changes
    document.getElementById('email').addEventListener('input', function() {
        if (emailVerified && this.value.trim() !== emailForVerification) {
            emailVerified = false;
            emailForVerification = '';
            document.getElementById('submitBtn').disabled = true;
            document.getElementById('otpVerificationSection').classList.add('hidden');
            document.getElementById('signupHint').textContent = 'Please verify your email first';
            document.getElementById('signupHint').classList.remove('text-green-600', 'dark:text-green-400');
            document.getElementById('signupHint').classList.add('text-gray-500', 'dark:text-gray-400');
            updateEmailVerificationStatus('', '');
        }
    });
    
    function updateEmailVerificationStatus(message, className) {
        const statusEl = document.getElementById('emailVerificationStatus');
        if (message) {
            statusEl.textContent = message;
            statusEl.className = 'mt-1 text-xs ' + className;
            statusEl.classList.remove('hidden');
        } else {
            statusEl.classList.add('hidden');
        }
    }
    
    document.getElementById('registerForm').addEventListener('submit', async (e) => {
        e.preventDefault();
        
        // Check if email is verified
        if (!emailVerified) {
            showError('Please verify your email before creating an account');
            return;
        }
        
        // Hide previous messages
        document.getElementById('errorMessage').classList.add('hidden');
        document.getElementById('successMessage').classList.add('hidden');
        
        // Validate form
        if (!validateForm()) {
            return;
        }
        
        const formData = new FormData(e.target);
        const data = Object.fromEntries(formData);
        
        // Remove confirmPassword and terms from data
        delete data.confirmPassword;
        delete data.terms;
        
        // Phone number is already cleaned by input validation, but ensure it's 10 digits
        if (data.phone) {
            data.phone = data.phone.replace(/\D/g, '');
            // Prepend +91 for storage/API
            data.phone = '+91' + data.phone;
        }
        
        setLoading(true);
        
        try {
            const response = await fetch('/api/auth/register', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(data)
            });
            
            const result = await response.json();
            
            if (response.ok) {
                if (result.token) {
                    localStorage.setItem('token', result.token);
                }
                showSuccess('Account created successfully! Redirecting...');
                setTimeout(() => {
                    if (result.user && result.user.role === 'admin') {
                        window.location.href = '/admin/dashboard';
                    } else {
                        window.location.href = '/dashboard';
                    }
                }, 500);
            } else {
                showError(result.error || 'Registration failed. Please try again.');
                setLoading(false);
            }
        } catch (error) {
            console.error('Registration error:', error);
            showError('An error occurred. Please check your connection and try again.');
            setLoading(false);
        }
    });
    
    // Real-time password match validation
    document.getElementById('confirmPassword').addEventListener('input', function() {
        const password = document.getElementById('password').value;
        const confirmPassword = this.value;
        
        if (confirmPassword && password !== confirmPassword) {
            this.classList.add('border-red-500', 'dark:border-red-500');
            this.classList.remove('border-gray-300', 'dark:border-gray-600');
        } else {
            this.classList.remove('border-red-500', 'dark:border-red-500');
            this.classList.add('border-gray-300', 'dark:border-gray-600');
        }
    });
    
    // Check if user is already logged in
    document.addEventListener('DOMContentLoaded', function() {
        const token = localStorage.getItem('token');
        if (token) {
            // Verify token is still valid
            fetch('/api/auth/me', {
                headers: { 'Authorization': 'Bearer ' + token }
            })
            .then(res => {
                if (res.ok) {
                    window.location.href = '/dashboard';
                } else {
                    localStorage.removeItem('token');
                }
            })
            .catch(() => {
                localStorage.removeItem('token');
            });
        }
    });
</script>
` }) %>

