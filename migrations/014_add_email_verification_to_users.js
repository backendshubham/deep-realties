exports.up = function () {
  // Legacy migration placeholder.
  // Original file is missing; this no-op keeps the migration sequence consistent
  // without changing the existing database schema.
  return Promise.resolve();
};

exports.down = function () {
  // No-op rollback matching the placeholder up migration.
  return Promise.resolve();
};

